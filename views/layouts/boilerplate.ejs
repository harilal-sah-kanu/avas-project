<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Avas</title>
  <link rel="icon" type="image/x-icon" href="/images/avas.ico">
  <link rel="stylesheet" href="/css/style.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Manrope:wght@200..800&family=Plus+Jakarta+Sans:ital,wght@0,200..800;1,200..800&family=Poppins:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400;1,500;1,600&display=swap"
    rel="stylesheet">
  <link rel="stylesheet" href="/css/rating.css">
  <link rel="stylesheet" href="/css/navbar.css">
  <link rel="stylesheet" href="/css/flash.css">
</head>

<body>
  <%- include("../includes/navbar") %>
    <div class="container">
      <%- include("../includes/flash") %>
        <%- body %>
    </div>
    <%- include("../includes/footer") %>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
      <script src="/js/script.js"></script>
      <script>
        // Limit image size on frontend (2MB)
        document.addEventListener('DOMContentLoaded', function () {
          const imageInput = document.getElementById('image');
          if (imageInput) {
            imageInput.addEventListener('change', function (e) {
              if (this.files[0] && this.files[0].size > 2 * 1024 * 1024) {
                alert('Image size must be less than 2MB!');
                this.value = '';
              }
            });
          }
          const editImageInput = document.getElementById('img');
          if (editImageInput) {
            editImageInput.addEventListener('change', function (e) {
              if (this.files[0] && this.files[0].size > 2 * 1024 * 1024) {
                alert('Image size must be less than 2MB!');
                this.value = '';
              }
            });
          }
        });
      </script>

</html>